# Phase-4

Phase 4 of 6

* Quantization Math: Convert 32-bit floating-point weights (FP32) into 8-bit or 4-bit integers without destroying the network's predictive accuracy.
* CUDA & C++ Kernels: Step outside of Python to write custom matrix multiplication directly on GPU hardware, managing SRAM, threads, and warps.
* Distributed Training: Master 3D Parallelism (Data, Tensor, and Pipeline parallelism) to mathematically split a single massive model across hundreds of GPUs.
* Speculative Decoding: Code a pipeline where a tiny "Draft" model predicts the next 5 tokens instantly, and a massive "Target" model mathematically verifies them in a single parallel forward pass to break sequential bottlenecks.

[havnt started]
